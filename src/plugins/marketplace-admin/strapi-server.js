'use strict';

module.exports = ({ strapi }) => ({
  register(/* { strapi } */) {
    // Enregistrement du plugin
  },

  bootstrap(/* { strapi } */) {
    // Initialisation du plugin
  },

  destroy(/* { strapi } */) {
    // Nettoyage du plugin
  },
});
